{% set name = "pyIGRF" %}
{% set version = "0.3.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: "10b7b678b63329579424a53847f3a1c78bc2d2debc715ebf5648f3c5e115a6f2"

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "
  noarch: python

requirements:
  host:
    - pip
    - python
  run:
    - numpy
    - python

test:
  imports:
    - pyIGRF

about:
  home: "https://github.com/zzyztyy/pyIGRF"
  license: "MIT"
  license_family: "MIT"
  license_file: "LICENSE"
  summary: "IGRF-12 Model by Python"
  dev_url: "https://github.com/zzyztyy/pyIGRF"

extra:
  recipe-maintainers:
    - jordan-heemskerk
